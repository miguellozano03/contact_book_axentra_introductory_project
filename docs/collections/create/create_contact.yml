info:
  name: create_contact
  type: http
  seq: 3

http:
  method: POST
  url: "{{host}}/api/v1/contacts"
  body:
    type: json
    data: |-
      {
        "first_name": "Satoshi",
        "last_name": "Nakamoto",
        "phone_no": "+810901234",
        "email": "p2p@bitcoin.me",
        "description": "Cliente legendario, paga siempre en BTC.",
        "outlet_id": "OUT-001"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
