info:
  name: delete_contact
  type: http
  seq: 2

http:
  method: DELETE
  url: "{{host}}/api/v1/contacts/1"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
