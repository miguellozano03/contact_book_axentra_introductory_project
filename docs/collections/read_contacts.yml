info:
  name: read_contacts
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}/api/v1/contacts"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
